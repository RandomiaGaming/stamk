{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Stamk (Build and Debug)",
      "type": "cppvsdbg",
      "request": "launch",
      "cwd": "${workspaceFolder}/bin",
      "program": "${workspaceFolder}/bin/*",
      "preLaunchTask": "stamk_build_task"
    }
  ]
}