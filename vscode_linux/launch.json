{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "PowerCues (Build and Debug)",
      "type": "cppvsdbg",
      "request": "launch",
      "cwd": "${workspaceFolder}/bin",
      "program": "${workspaceFolder}/bin/powercues",
      "preLaunchTask": "powercues_build_task"
    }
  ]
}